# Frontend Next.js (SSR) Demo

This folder contains a minimal Next.js 13 app demonstrating SSR, a typed service layer, Tailwind styling, and a unit test.

Quick start:

```bash
cd frontend-next
npm install
npm run dev
```

Env:

```
NEXT_PUBLIC_API_URL=http://localhost:8000
```
